name: CI

on:
  push:
    branches: [main, master]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  migration-tests:
    name: Database migrations (PostgreSQL ${{ matrix.postgres }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - postgres: "15"
            image: postgres:15-alpine@sha256:fe0737ba566a2c5b2a28f34433c0a423261900ec17b9bf7ad115e1aae7e57f1b
          - postgres: "16"
            image: postgres:16-alpine@sha256:cf78e76683b9ca8c5733cbbdce6c9262b45b6767934dd0a95e671f9a0fc20685
    services:
      postgres:
        image: ${{ matrix.image }}
        env:
          POSTGRES_USER: moyro_migration_test
          POSTGRES_PASSWORD: moyro_migration_test_password
          POSTGRES_DB: moyro_migration_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U moyro_migration_test -d moyro_migration_test"
          --health-interval 2s
          --health-timeout 5s
          --health-retries 15
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: server/go.mod
          cache-dependency-path: server/go.sum
      - name: Verify migration lifecycle
        working-directory: server
        env:
          MOYRO_TEST_POSTGRES_DSN: postgres://moyro_migration_test:moyro_migration_test_password@127.0.0.1:5432/moyro_migration_test?sslmode=disable
        run: |
          go test -race ./internal/store ./internal/auth ./internal/files ./internal/scheduled ./internal/webhooks
          go vet ./internal/store ./internal/auth ./internal/files ./internal/scheduled ./internal/webhooks

  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: server/go.mod
          cache-dependency-path: server/go.sum
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: webapp/package-lock.json
      - name: Install web dependencies
        working-directory: webapp
        run: npm ci
      - name: Enforce source-size ratchets
        run: bash scripts/check-source-sizes.sh
      - name: Audit production web dependencies
        working-directory: webapp
        run: |
          npm audit --omit=dev --audit-level=high
          npm audit --audit-level=high
      - name: Type-check and build webapp
        working-directory: webapp
        run: |
          npm run typecheck
          npm run build
      - name: Test server
        working-directory: server
        run: |
          go test -race ./...
          go vet ./...
          go run golang.org/x/vuln/cmd/govulncheck@v1.7.0 ./...
      - name: Test Go plugin SDK
        working-directory: plugin-sdk/go
        run: |
          go test -race ./...
          go vet ./...
      - name: Build service image
        run: |
          docker build \
            --build-arg VERSION=ci \
            --build-arg COMMIT="${GITHUB_SHA}" \
            --build-arg BUILD_DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
            --tag moyro:ci \
            .
      - name: Install Chromium for product-page tests
        working-directory: webapp
        run: npx playwright install --with-deps chromium
      - name: Verify routed pages, refresh, mobile layout, and browser errors
        env:
          MOYRO_E2E_PORT: "18065"
        run: |
          docker pull postgres:16-alpine@sha256:cf78e76683b9ca8c5733cbbdce6c9262b45b6767934dd0a95e671f9a0fc20685
          bash scripts/verify-product-ui.sh moyro:ci "${RUNNER_TEMP}/moyro-screens"
      - name: Verify GitHub Pages assets and structured data
        run: node scripts/verify-pages.mjs
